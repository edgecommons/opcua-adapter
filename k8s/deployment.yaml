# Minimal Kubernetes Deployment for a edgecommons component. With --platform auto the library detects
# KUBERNETES from the ServiceAccount token; config source defaults to CONFIGMAP, transport to MQTT
# (broker config from the mounted ConfigMap), identity from the Downward API — so NO args are needed.
# Build the image from ./Dockerfile and push to your registry (e.g. ghcr.io/<owner>/opcua-adapter)
# or `kind load` it for a local cluster, then set `image:` below.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opcua-adapter
  labels:
    app.kubernetes.io/name: opcua-adapter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: opcua-adapter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: opcua-adapter
    spec:
      automountServiceAccountToken: true   # the projected SA token is the platform=KUBERNETES auto-detect signal
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
      containers:
        - name: component
          image: REPLACE_ME   # e.g. ghcr.io/<owner>/opcua-adapter:latest (build ./Dockerfile)
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_NAME
              valueFrom: { fieldRef: { fieldPath: metadata.name } }
            - name: POD_NAMESPACE
              valueFrom: { fieldRef: { fieldPath: metadata.namespace } }
            - name: NODE_NAME
              valueFrom: { fieldRef: { fieldPath: spec.nodeName } }
          ports:
            - { name: health, containerPort: 8081 }
            - { name: metrics, containerPort: 9090 }
          startupProbe:
            httpGet: { path: /startupz, port: health }
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet: { path: /livez, port: health }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /readyz, port: health }
            periodSeconds: 10
          workingDir: /tmp   # Java Paho writes its file-persistence dir in cwd; keep cwd on the writable tmp volume
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: config, mountPath: /etc/edgecommons, readOnly: true }   # WHOLE volume, never subPath
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: config
          configMap: { name: opcua-adapter-config }
        - name: tmp
          emptyDir: {}
